<% if @item.destroyed? %>
  $('#item-' +<%= @item.id %>).fadeOut(800);
  $('.js-items-count').html("<%= pluralize(@item.user.items.count, 'item') %>");
<% else %>
  $('#item-' +<%= @item.id %>).prepend("<div class='alert alert-danger'><%= flash[:error] %></div>");
<% end %>